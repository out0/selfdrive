@PACKAGE_INIT@

include("${CMAKE_CURRENT_LIST_DIR}/drivelessTargets.cmake")

# Optionally set variables for consumers
set(DRIVELESS_INCLUDE_DIRS "${CMAKE_INSTALL_PREFIX}/${CMAKE_INSTALL_INCLUDEDIR}/driveless")
set(DRIVELESS_LIBRARIES driveless)
